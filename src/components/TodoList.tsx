//  투두 아이템을 임포트해서 반복해주는 프레젠테이션 컴포넌트
import { FC } from "react";

const TodoList: FC = () => {
  return <></>;
};
// FC 펑셔널 컴포넌트 타입.
// NFC 지금은 안쓰는데 props 를 받지않는 정적 함수형 컴포넌트. 여기까지만
export default TodoList;
